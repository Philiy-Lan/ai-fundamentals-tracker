---
phase: 03-flashcards-and-quiz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/content/flashcards.js
  - src/data/content/quizzes.js
  - src/__tests__/content.flashcards.test.js
  - src/__tests__/content.quizzes.test.js
  - src/__tests__/FlashcardViewer.test.jsx
  - src/__tests__/QuizViewer.test.jsx
  - src/__tests__/ActivityPanel.test.jsx
autonomous: true
requirements:
  - CONT-03
  - CONT-04
  - CONT-05
  - CONT-06

must_haves:
  truths:
    - "All 8 modules have flashcard data with at least 5 cards each"
    - "All 8 modules have quiz data with at least 5 questions each"
    - "Test stubs exist in RED state for FlashcardViewer and QuizViewer"
    - "Content tests verify all modules have populated data with correct shapes"
  artifacts:
    - path: "src/data/content/flashcards.js"
      provides: "Flashcard Q&A pairs for all 8 modules"
      contains: "FLASHCARDS"
    - path: "src/data/content/quizzes.js"
      provides: "Quiz questions for all 8 modules"
      contains: "QUIZZES"
    - path: "src/__tests__/FlashcardViewer.test.jsx"
      provides: "Test stubs for FLASH-01 through FLASH-06"
      min_lines: 50
    - path: "src/__tests__/QuizViewer.test.jsx"
      provides: "Test stubs for QUIZ-01 through QUIZ-06"
      min_lines: 50
  key_links:
    - from: "src/__tests__/content.flashcards.test.js"
      to: "src/data/content/flashcards.js"
      via: "import FLASHCARDS"
      pattern: "import.*FLASHCARDS.*from"
    - from: "src/__tests__/content.quizzes.test.js"
      to: "src/data/content/quizzes.js"
      via: "import QUIZZES"
      pattern: "import.*QUIZZES.*from"
---

<objective>
Populate flashcard and quiz content for all 8 modules, update content tests, and create Wave 0 test stubs for FlashcardViewer and QuizViewer components.

Purpose: Establishes all data and test infrastructure before component implementation begins in Wave 2.
Output: Populated content files, updated content tests (GREEN), component test stubs (RED).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-flashcards-and-quiz/03-CONTEXT.md
@.planning/phases/03-flashcards-and-quiz/03-RESEARCH.md

<interfaces>
<!-- Existing data shapes from scaffold files -->

From src/data/content/flashcards.js:
```javascript
export const FLASHCARDS = {
  "1": [
    { question: "What is the difference between narrow AI and general AI?", answer: "Narrow AI handles a specific task..." },
    // ...
  ],
  "2": [],
  // ... through "8"
}
```

From src/data/content/quizzes.js:
```javascript
export const QUIZZES = {
  "1": [
    { question: "Which of the following is an example of narrow AI?", options: ["A robot...", "A chess engine...", ...], correctIndex: 1 },
    // ...
  ],
  "2": [],
  // ... through "8"
}
```

From src/data/modules.js (module titles for content generation):
```javascript
// Module 1: "What is AI?"
// Module 2: "Machine Learning Essentials"
// Module 3: "Neural Networks & Deep Learning"
// Module 4: "Natural Language Processing"
// Module 5: "Computer Vision"
// Module 6: "Generative AI & LLMs"
// Module 7: "AI Ethics & Safety"
// Module 8: "AI in Practice"
```

From src/components/ActivityPanel.jsx (component interface pattern):
```javascript
// Content components receive: { moduleId: string, onComplete: () => void }
case "audio":
  return <AudioPlayer moduleId={moduleId} onComplete={onComplete} />
```

From src/__tests__/ActivityPanel.test.jsx (mock pattern):
```javascript
vi.mock("../components/AudioPlayer.jsx", () => ({
  AudioPlayer: () => <div data-testid="audio-player-mock">AudioPlayer</div>,
}))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate flashcard and quiz content for all 8 modules + update content tests</name>
  <files>
    src/data/content/flashcards.js,
    src/data/content/quizzes.js,
    src/__tests__/content.flashcards.test.js,
    src/__tests__/content.quizzes.test.js
  </files>
  <read_first>
    src/data/content/flashcards.js,
    src/data/content/quizzes.js,
    src/__tests__/content.flashcards.test.js,
    src/__tests__/content.quizzes.test.js,
    src/data/modules.js
  </read_first>
  <action>
    **Content extraction:** Attempt to use notebooklm-mcp MCP tools to extract flashcard Q&A pairs (CONT-03) and quiz multiple-choice questions (CONT-04) from all 8 NotebookLM notebooks. Per D-01 and D-02.

    **Fallback (per D-05):** If MCP extraction is unavailable in the executor context, generate placeholder content that is structurally valid and topically accurate for each module. Each module MUST have at least 8 flashcards and 8 quiz questions. Content should be based on the module titles from modules.js:
    - Module 1: "What is AI?" (already has 3 cards/questions — expand to 8+)
    - Module 2: "Machine Learning Essentials"
    - Module 3: "Neural Networks and Deep Learning"
    - Module 4: "Natural Language Processing"
    - Module 5: "Computer Vision"
    - Module 6: "Generative AI and LLMs"
    - Module 7: "AI Ethics and Safety"
    - Module 8: "AI in Practice"

    **Data shapes (per D-04):**
    - Flashcards: `{ question: string, answer: string }` — keep existing shape exactly
    - Quizzes: `{ question: string, options: string[], correctIndex: number }` — 4 options each, correctIndex 0-3

    **Organize per D-03/CONT-05:** Populate the existing scaffold files directly. Do NOT create new files. Keep the `FLASHCARDS` and `QUIZZES` named exports.

    **CONT-06 note:** This requirement is about audio compression (not applicable to JS data). Mark as N/A — no action needed since flashcard/quiz data is JS, not audio.

    **Update content tests:**
    - In `content.flashcards.test.js`: Change the "modules 2-8 are empty arrays" test to verify all 8 modules have 2+ cards with correct `{ question, answer }` shape (same pattern as the module 1 test).
    - In `content.quizzes.test.js`: Change the "modules 2-8 are empty arrays" test to verify all 8 modules have 2+ questions with correct `{ question, options, correctIndex }` shape.
  </action>
  <verify>
    <automated>cd /Users/philiy/Developer/ai-tracker && npm test -- --reporter=verbose src/__tests__/content.flashcards.test.js src/__tests__/content.quizzes.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "question" src/data/content/flashcards.js` returns 64 or more (8 modules x 8+ cards)
    - `grep -c "correctIndex" src/data/content/quizzes.js` returns 64 or more (8 modules x 8+ questions)
    - `grep "modules 2-8 are empty" src/__tests__/content.flashcards.test.js` returns NO matches (old test removed)
    - `grep "modules 2-8 are empty" src/__tests__/content.quizzes.test.js` returns NO matches (old test removed)
    - `grep "all 8 modules have" src/__tests__/content.flashcards.test.js` returns a match (new test exists)
    - Content tests pass: `npm test -- src/__tests__/content.flashcards.test.js src/__tests__/content.quizzes.test.js` exits 0
  </acceptance_criteria>
  <done>All 8 modules have flashcard and quiz content with correct shapes. Content tests are GREEN verifying all modules are populated.</done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 test stubs for FlashcardViewer, QuizViewer, and extend ActivityPanel tests</name>
  <files>
    src/__tests__/FlashcardViewer.test.jsx,
    src/__tests__/QuizViewer.test.jsx,
    src/__tests__/ActivityPanel.test.jsx
  </files>
  <read_first>
    src/__tests__/ActivityPanel.test.jsx,
    src/components/ActivityPanel.jsx,
    src/components/DeckViewer.jsx,
    .planning/phases/03-flashcards-and-quiz/03-RESEARCH.md
  </read_first>
  <action>
    **Create `src/__tests__/FlashcardViewer.test.jsx`** with failing test stubs (RED state) for:
    - FLASH-01: "renders question text and flips to answer on card click" — render FlashcardViewer with moduleId="1", verify question text visible, click the card, verify answer text visible
    - FLASH-02: "shows Got it and Missed it buttons only after flip" — before flip: no rating buttons. After flip: both buttons visible
    - FLASH-03: "increments score on Got it rating" — flip card, click "Got it", verify score display shows "1 / 3 correct" (module 1 has 3+ cards)
    - FLASH-04: "shows card progress indicator Card X of Y" — verify text matching pattern "Card 1 of" is visible on render
    - FLASH-05: "shuffle toggle randomizes card order" — render, note first card question, click shuffle button, verify card order changed (or at minimum the shuffle state toggled)
    - FLASH-06: "calls onComplete after all cards are rated" — mock onComplete, rate all cards, verify onComplete called once

    Mock framer-motion AnimatePresence to avoid animation issues in tests:
    ```javascript
    vi.mock("framer-motion", () => ({
      AnimatePresence: ({ children }) => children,
      motion: {
        div: ({ children, ...props }) => <div {...props}>{children}</div>,
      },
    }))
    ```

    Import the component as: `import { FlashcardViewer } from "../components/FlashcardViewer.jsx"`
    Import flashcard data: `import { FLASHCARDS } from "../data/content/flashcards.js"`

    **Create `src/__tests__/QuizViewer.test.jsx`** with failing test stubs (RED state) for:
    - QUIZ-01: "renders question text and selectable options" — render QuizViewer with moduleId="1", verify question text and all 4 options visible
    - QUIZ-02: "Submit button shows correct/incorrect feedback" — select an option, click Submit, verify color feedback applied
    - QUIZ-03: "reveals correct answer highlighted green on wrong selection" — select wrong option, submit, verify correct option has green styling
    - QUIZ-04: "shows question progress Q X of Y" — verify text matching "Q 1 of" is visible
    - QUIZ-05: "shows final score screen with percentage after last question" — answer all questions, verify score screen with percentage visible
    - QUIZ-06: "calls onComplete when Done is clicked on score screen" — complete quiz, click Done, verify onComplete called once

    Mock pattern same as FlashcardViewer (mock framer-motion if QuizViewer uses it).

    **Extend `src/__tests__/ActivityPanel.test.jsx`:**
    - Add vi.mock stubs for both new components:
      ```javascript
      vi.mock("../components/FlashcardViewer.jsx", () => ({
        FlashcardViewer: () => <div data-testid="flashcard-viewer-mock">FlashcardViewer</div>,
      }))
      vi.mock("../components/QuizViewer.jsx", () => ({
        QuizViewer: () => <div data-testid="quiz-viewer-mock">QuizViewer</div>,
      }))
      ```
    - Add test: "renders FlashcardViewer when activity.id is 'flashcards' and isOpen is true (D-22)" — expects `screen.getByTestId("flashcard-viewer-mock")`. This test will FAIL until Plan 04 wires ActivityPanel.
    - Add test: "renders QuizViewer when activity.id is 'quiz' and isOpen is true (D-22)" — expects `screen.getByTestId("quiz-viewer-mock")`. This test will also FAIL until Plan 04.
    - Update the existing test "renders placeholder for unimplemented activities (flashcards)" — change the activity.id to "teachback" since flashcards will no longer be a placeholder. Update description to say "(teachback)" instead.

    All FlashcardViewer and QuizViewer tests should FAIL (RED) because the component files do not exist yet.
    ActivityPanel new routing tests should FAIL because the switch cases are not added yet.
    Existing ActivityPanel tests MUST still PASS.
  </action>
  <verify>
    <automated>cd /Users/philiy/Developer/ai-tracker && npm test -- --reporter=verbose src/__tests__/FlashcardViewer.test.jsx 2>&1 | tail -5; echo "---"; npm test -- --reporter=verbose src/__tests__/QuizViewer.test.jsx 2>&1 | tail -5; echo "---"; npm test -- --reporter=verbose src/__tests__/ActivityPanel.test.jsx 2>&1 | grep -E "(PASS|FAIL|Tests)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `ls src/__tests__/FlashcardViewer.test.jsx` succeeds
    - File exists: `ls src/__tests__/QuizViewer.test.jsx` succeeds
    - `grep "FLASH-01" src/__tests__/FlashcardViewer.test.jsx` returns a match
    - `grep "FLASH-06" src/__tests__/FlashcardViewer.test.jsx` returns a match
    - `grep "QUIZ-01" src/__tests__/QuizViewer.test.jsx` returns a match
    - `grep "QUIZ-06" src/__tests__/QuizViewer.test.jsx` returns a match
    - `grep "flashcard-viewer-mock" src/__tests__/ActivityPanel.test.jsx` returns a match
    - `grep "quiz-viewer-mock" src/__tests__/ActivityPanel.test.jsx` returns a match
    - `grep "teachback" src/__tests__/ActivityPanel.test.jsx` returns a match for the updated placeholder test
    - FlashcardViewer tests fail (RED — component does not exist yet)
    - QuizViewer tests fail (RED — component does not exist yet)
    - Existing ActivityPanel tests that were passing before still pass
  </acceptance_criteria>
  <done>Test stubs exist for all FLASH and QUIZ requirements in RED state. ActivityPanel test file extended with mock stubs and routing tests for flashcards/quiz. Existing passing tests remain green.</done>
</task>

</tasks>

<verification>
- Content tests pass: `npm test -- src/__tests__/content.flashcards.test.js src/__tests__/content.quizzes.test.js`
- FlashcardViewer test file exists and fails (RED): `npm test -- src/__tests__/FlashcardViewer.test.jsx` exits non-zero
- QuizViewer test file exists and fails (RED): `npm test -- src/__tests__/QuizViewer.test.jsx` exits non-zero
- Existing tests still pass: `npm test -- src/__tests__/ActivityPanel.test.jsx` (existing tests green, new routing tests may fail)
</verification>

<success_criteria>
- All 8 modules have 8+ flashcards and 8+ quiz questions with correct data shapes
- Content tests are GREEN for all modules
- Test stubs exist in RED state covering FLASH-01 through FLASH-06 and QUIZ-01 through QUIZ-06
- ActivityPanel test file has mock stubs and routing tests for both new components
- No existing passing tests are broken
</success_criteria>

<output>
After completion, create `.planning/phases/03-flashcards-and-quiz/03-01-SUMMARY.md`
</output>
